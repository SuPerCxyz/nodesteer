# Implementation Plan

1. Pin and inspect the shadcn-admin source and preserve its license.
2. Fix route nesting and shared DataTable behavior.
3. Apply role-based column sizing, truncation, alignment and pagination to list pages.
4. Fix transfer/editor grids, detail data presentation, localized values and upload control.
5. Verify Light/Dark, responsive layouts and critical navigation on KVM2.
6. Run format, lint, typecheck, build, Go tests/lint and record the Playwright environment limitation.
