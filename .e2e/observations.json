{
  "schema_version": 1,
  "observations": [
    {
      "id": "OBS-001",
      "type": "possible_gap",
      "description": "Node detail allows adding a label but exposes no visible remove/edit action; cleanup required an authenticated API mutation.",
      "related_features": ["agent.detail", "agent.status"],
      "follow_up_required": true,
      "resolved": false
    },
    {
      "id": "OBS-002",
      "type": "environmental",
      "description": "The transfer negative path correctly retained FAILED state and exposed retry, but the intentionally selected source path was absent on the source Agent; no destructive cleanup action is available for the failed transfer.",
      "related_features": ["transfer.create", "transfer.retry"],
      "follow_up_required": false,
      "resolved": true
    },
    {
      "id": "OBS-003",
      "type": "possible_gap",
      "description": "After deleting a task, its execution remains in history and is displayed as localized 未知任务; the execution ID remains available as secondary technical metadata and the node is shown by hostname.",
      "related_features": ["task.delete", "execution.list", "execution.detail"],
      "follow_up_required": false,
      "resolved": true
    }
  ]
}
